
########################
Your {{ .Chart.Name }} application has been deployed to the {{ .Release.Namespace }} namespace.

########################

🔑 Important Configuration
1. API Key: Ensure your API_KEY (currently {{ .Values.API_KEY }}) is correctly set in your values.yaml or via --set. This is crucial for the agent to communicate with the Kloudmate backend.

2. Collector Endpoint: The COLLECTOR_ENDPOINT (currently {{ .Values.COLLECTOR_ENDPOINT | default "not set" }}) should point to your Kloudmate backend or an intermediate collector.

3. Configuration Check Interval: The agent checks for configuration updates every {{ .Values.KM_CONFIG_CHECK_INTERVAL }}.

########################

🌐 OpenTelemetry Operator (If Enabled)
{{- if (index .Values "opentelemetry-operator" | default dict).enabled }}
The OpenTelemetry Operator has been installed (or is managed) as part of this chart.

CRDs: The necessary OpenTelemetry CRDs are managed ({{ if ((index .Values "opentelemetry-operator" | default dict).crds | default dict).create }}and created{{ else }}but not created by this chart{{ end }}).

Admission Webhooks: Admission webhooks are {{ if ((index .Values "opentelemetry-operator" | default dict).admissionWebhooks | default dict).create }}enabled{{ else }}disabled{{ end }} for sidecar injection and other functionalities.
{{- else }}
The OpenTelemetry Operator is not enabled or installed by this chart (opentelemetry-operator.enabled: false).
{{- end }}

########################

📜 Further Information
Kloudmate Agent Documentation: [https://docs.kloudmate.com/{{ .Chart.Name }}]
Kubernetes DaemonSets: [https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/]
Thank you for installing {{ .Chart.Name }}!